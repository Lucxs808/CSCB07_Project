<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.Dark"
        tools:targetApi="31">
        <activity
            android:name=".Register"
            android:theme="@style/Theme.Material3.Dark.NoActionBar"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:theme="@style/Theme.Material3.Dark.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Material3.Dark.NoActionBar"
            android:exported="true"/>
        <activity
            android:name=".AdminPage"
            android:theme="@style/Theme.Material3.Dark.NoActionBar"
            android:exported="true"/>
        <activity
            android:name=".InputGrades"
            android:theme="@style/Theme.Material3.Dark.NoActionBar"
            android:exported="true" />
        <activity
            android:name=".POStChecker"
            android:theme="@style/Theme.Material3.Dark.NoActionBar"
            android:exported="true" />
        <activity
            android:name=".AnnounceEventActivity"
            android:label="Announce Event">
        </activity>
        <activity
            android:name=".ViewEventsActivity"
            android:label="View Events">
        </activity>
        <activity
            android:name=".AdminAnnounce"
            android:label="Make Announcement" />
        <activity
            android:name=".ViewAnnouncements"
            android:label="View Announcements" />
        <activity
            android:name=".NotificationBoard"
            android:label="View Notifications" />
        <activity
            android:name=".SubmitComplaint"
            android:label="Submit Complaint" />

    </application>

</manifest>